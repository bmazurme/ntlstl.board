import {
  getBlocks, addBlock, deleteBlock, renameBlock, setBlocks, updateBlocks,
} from './blocks-controller';

export {
  getBlocks,
  updateBlocks,
  deleteBlock,
  addBlock,
  renameBlock,
  setBlocks,
};
